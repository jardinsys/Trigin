//On welcome in server

/*guildProfile = await new Guild({
        _id: mongoose.Types.ObjectId(),
        guildId: interaction.guild.id,
        guildSponserCount: 0,
      });*/
      
//Ask what channel to complete set up
//Ask what role(s) are admins (only ADMINIISTRATOR can edit)
//Ask what member(s) are admins (only ADMINIISTRATOR can edit)
//Elaborate that editing the server's trigger list set up can happen in /trigger 
//      - and more options are available in settings
//      - help and ways tp support cam be om /support